import React, { Fragment } from 'react'
import card from './card.gif';

const Card = () => <Fragment>
      <img src={card} alt="last card" style={{ width: '400px', margin: 'auto', display: 'block' }} />
    </Fragment>

export default Card;
